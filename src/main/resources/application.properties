server.port=8081
spring.application.name=ghana-transport-api

# Disable Spring Boot's default banner (optional)
spring.main.banner-mode=console

# Increase response size for full graph (551 edges)
server.tomcat.max-http-response-header-size=65536
